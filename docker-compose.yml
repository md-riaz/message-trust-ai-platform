version: '3.8'
services:
  message-trust-ai-platform:
    build: .
    container_name: message-trust-ai-platform
    volumes:
      - ./app/models:/app/app/models
    ports:
      - "8093:8000"
    restart: unless-stopped
